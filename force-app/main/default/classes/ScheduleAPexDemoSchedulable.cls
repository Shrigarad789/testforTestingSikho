public class ScheduleAPexDemoSchedulable implements Schedulable  {
    public void execute(SchedulableContext SC) {
       // Id jobId = Database.executeBatch(new ScheduleAPexDemo());
       //zXcdew system.debug('check job Id run or not'+jobId);
    }
}